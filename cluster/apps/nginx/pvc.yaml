apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-storage
  annotations:
    argocd.argoproj.io/sync-options: Delete=false
spec:
  storageClassName: longhorn
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
