# Velero B2 Credentials SealedSecret Template
# 
# To create the actual sealed secret:
# 1. Create a plain secret file (DO NOT COMMIT):
#    cat > /tmp/b2-credentials.yaml <<EOF
#    apiVersion: v1
#    kind: Secret
#    metadata:
#      name: velero-b2-credentials
#      namespace: velero
#    stringData:
#      cloud: |
#        [default]
#        aws_access_key_id = YOUR_B2_KEY_ID
#        aws_secret_access_key = YOUR_B2_APPLICATION_KEY
#    EOF
#
# 2. Seal the secret:
#    kubeseal --format=yaml --scope=cluster-wide \
#      --cert=/tmp/sealed-secrets-cert.pem \
#      < /tmp/b2-credentials.yaml > cluster/infrastructure/backup/velero/b2-credentials-sealed.yaml
#
# 3. Clean up the plain secret:
#    rm /tmp/b2-credentials.yaml
#
# 4. Commit only the sealed secret

# Placeholder - replace with actual sealed secret after running above commands
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  annotations:
    sealedsecrets.bitnami.com/cluster-wide: "true"
  name: velero-b2-credentials
  namespace: velero
spec:
  encryptedData:
    cloud: REPLACE_WITH_ENCRYPTED_CREDENTIALS
  template:
    metadata:
      annotations:
        sealedsecrets.bitnami.com/cluster-wide: "true"
      name: velero-b2-credentials
      namespace: velero
    type: Opaque
